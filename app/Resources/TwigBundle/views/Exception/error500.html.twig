{% extends 'base.html.twig' %}
{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h1 class="mt-5">{{ '500.head'|trans({}, 'exception') }}</h1>
                <p class="lead">{{ '500.description'|trans({}, 'exception') }}</p>
                <ul class="list-unstyled">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <li><a href="{{ path('panel') }}" class="btn btn-success">Powrót</a></li>
                    {% else %}
                        <li><a href="{{ path('front_index') }}" class="btn btn-success">Powrót</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
{% endblock %}