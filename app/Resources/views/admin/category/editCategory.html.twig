{% extends 'admin/adminlLayout.html.twig' %}

{% block title %}
    {{ 'admin.title.edit_category'|trans({}, 'controller') }}
{% endblock %}

{% block panelContent %}
    {% include 'template/flashMsg.html.twig' %}
    <div class="row bg-white pt-3">
    {% if app.request.get('_route') == 'admin_category_create' %}
        <div class="col-lg-9 float-left">
            <h3>{{ 'admin.category.create'|trans({}, 'controller') }}</h3>
        </div>
        <div class="col-lg-3">
        </div>
        {% else %}
            <div class="col-lg-9 float-left">
                <h3>{{ 'admin.title.edit_category'|trans({}, 'controller') }}</h3>
            </div>
            <div class="col-lg-3">
                <a href="{{ path('admin_category_delete', {"slug": category.slug}) }}" class="btn btn-warning">{{ 'admin.delete'|trans({}, 'controller') }}</a>
            </div>
        {% endif %}
    </div>
    <div class="link bg-white">
        <div class="row text-center">
            <div class="col-lg-12">
                {{ form(form) }}
                </div>
            </div>
        </div>

{% endblock %}